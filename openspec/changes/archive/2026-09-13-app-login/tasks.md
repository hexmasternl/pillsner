## 1. Preconditions and build setup (no scaffolding in this change)

- [x] 1.1 Verify the `app-welcome-screen` scaffold exists in `src/`: Gradle project with version catalog, `PillsnerApplication`, `MainActivity`, `PillsnerApp` with the navigation host, `PillsnerTheme` in `ui/theme`, `AppContainer` with its view model factory, and the `Settings` route. If any of it is missing, stop and apply `app-welcome-screen` first; this change creates no part of the scaffold
- [x] 1.2 Note whether `app-settings-language` has already replaced the Settings placeholder with a list of sections and whether `datastore-preferences` is already in the catalog; adapt tasks 1.3 and 6.3 to what exists — neither had landed yet: the Settings placeholder was still title-only and `datastore-preferences` was not in the catalog, so this change adds both
- [x] 1.3 Add to the version catalog and the app module, at the newest stable versions (re-check the release pages; design values as of 11 September 2026): `androidx.biometric:biometric` 1.1.0, `androidx.datastore:datastore-preferences` 1.2.1 (if absent), `androidx.lifecycle:lifecycle-process` 2.11.0 (same version as the existing lifecycle entries) — also added `androidx.fragment:fragment-ktx` 1.9.0 (required by `BiometricPrompt`'s `FragmentActivity` host) and `androidx.lifecycle:lifecycle-runtime-ktx` 2.11.0 (for `FLAG_SECURE` toggling in the activity)
- [x] 1.4 Change `MainActivity` to extend `FragmentActivity` (required by `BiometricPrompt`), keeping `enableEdgeToEdge()` and the existing `setContent` unchanged; confirm `assembleDebug` and `test` still run green from `src/` — confirmed

## 2. App lock domain layer (pure Kotlin, no Android dependencies)

- [x] 2.1 Define `LockState` (sealed: `Loading`, `Disabled`, `Unlocked`, `Locked` with optional cooldown end, `Recovering`) and `PinCredential` (salt and verifier as byte arrays)
- [x] 2.2 Define `PinVerifier` interface (`isAvailable()`, `create(pin)`, `verify(pin, credential)`) and a `Pin` value class that validates 4 to 6 decimal digits
- [x] 2.3 Define `LockPolicy` with `maxAttemptsPerWindow = 5`, `baseCooldown = 30s`, `maxCooldown = 5min`, `gracePeriod = 0` and `cooldownFor(consecutiveFailures)` implementing the doubling schedule capped at 5 minutes
- [x] 2.4 Define `AppLockRepository` interface exposing `settings: Flow<AppLockSettings>` and suspend functions to store credential, clear credential, set biometric flag, record failed attempt with cooldown end, reset attempts, and reconcile biometric availability
- [x] 2.5 Define a `Clock` abstraction and a `BiometricAvailability` interface (`status(): Available | NoneEnrolled | NoHardware | Unavailable`) — uses `java.time.Clock` directly rather than a new interface: it is already the JDK's own abstraction, has `Clock.systemDefaultZone()` for production and `Clock.fixed(...)` for tests, and `java.time` is natively available at `minSdk` 26
- [x] 2.6 Implement use cases `EnablePinLock`, `DisablePinLock`, `UnlockWithPin`, `RegisterFailedAttempt`, `SetBiometricUnlock`, `ResetLockAfterRecovery` and a `ResolveInitialLockState` use case (enabled and key present → Locked, enabled and key missing → Recovering, else Disabled)
- [x] 2.7 Unit tests for `Pin` validation, `LockPolicy.cooldownFor` (30s, 60s, 120s, 240s, 300s cap), `UnlockWithPin` success and failure paths, cooldown persistence across a simulated restart, `DisablePinLock` refusing biometrics and honouring cooldown, and `ResolveInitialLockState` for all three branches, using `FakePinVerifier`, `FakeAppLockRepository` and a test clock — 18 tests, all passing (`./gradlew testDebugUnitTest`)

## 3. App lock data layer

- [x] 3.1 Implement `KeystorePinVerifier`: generate or load a non-exportable HMAC-SHA256 key in `AndroidKeyStore` under a fixed alias, 16-byte `SecureRandom` salt, HMAC over `salt || pin`, constant-time comparison with `MessageDigest.isEqual`, `isAvailable()` checking the alias
- [x] 3.2 Implement `DataStoreAppLockRepository` on a dedicated `applock` Preferences DataStore with keys for enabled, biometric enabled, salt, verifier, consecutive failures, cooldown end millis and settings version 1
- [x] 3.3 Implement `AndroidBiometricAvailability` wrapping `BiometricManager.canAuthenticate(BIOMETRIC_WEAK)` and mapping results to the domain status
- [x] 3.4 Add `dataExtractionRules` (API 31+) and `fullBackupContent` resources excluding the `applock` DataStore file from cloud backup and device transfer, and reference them from the manifest
- [x] 3.5 Wire the three implementations into the existing `AppContainer` and register `AppLockViewModel` in its view model factory
- [x] 3.6 Instrumented tests: `KeystorePinVerifier` round trip (create then verify correct and wrong PIN), `isAvailable()` false after the alias is deleted, and `DataStoreAppLockRepository` persisting and clearing settings — written and compiling (`KeystorePinVerifierTest`, `DataStoreAppLockRepositoryTest`) and run: all three pass on the `pixel_7_-_api_36_0` emulator (API 36)

## 4. Lock state and enforcement

- [x] 4.1 Implement `AppLockViewModel` (activity scoped) exposing `AppLockUiState` derived from `LockState`, remaining cooldown from the injected clock, biometric availability, and one-shot events for showing the biometric prompt
- [x] 4.2 Implement `LockOnBackgroundObserver` on `ProcessLifecycleOwner` that moves the state to `Locked` on `ON_STOP` when the lock is enabled, and register it from `PillsnerApplication`
- [x] 4.3 Resolve the initial state on cold start via `ResolveInitialLockState` before any content is composed, showing a blank surface while `Loading` — resolved once in `AppContainer`'s init, shared through `AppLockStateHolder`
- [x] 4.4 Update the existing `PillsnerApp` so the navigation suite and `NavHost` are composed only in `Unlocked` and `Disabled`; `Locked` and `Recovering` render `UnlockScreen`; `Loading` renders a blank `surface`; the `NavController` is created above the branch so navigation state survives a relock
- [x] 4.5 In `MainActivity`, collect the enabled flag and set or clear `FLAG_SECURE` on the window accordingly
- [x] 4.6 Implement `BiometricAuthenticator` in the activity: a suspend wrapper over `BiometricPrompt` returning `Success`, `Cancelled`, `LockedOut`, `Unavailable` or `Failed`, supporting `BIOMETRIC_WEAK` with a "Use PIN" negative button and a `DEVICE_CREDENTIAL | BIOMETRIC_WEAK` variant for recovery
- [x] 4.7 Unit tests for `AppLockViewModel`: locked on `ON_STOP` only when enabled, biometric prompt requested once per lock episode, no re-prompt after cancel or lockout, cooldown countdown from the test clock, recovery state when the verifier is unavailable — the `ON_STOP` behaviour is `LockOnBackgroundObserver`'s (tested there); `AppLockViewModel`'s own 6 tests use the same `viewModelScope`/`UnconfinedTestDispatcher` pattern as `HomeViewModelTest`, parking the view model in a `ViewModelStore` so its infinite cooldown ticker is cancelled at teardown

## 5. Unlock screen

- [x] 5.1 Build `PinKeypad` with the `pillsner-ui-build` skill: digits 0 to 9 and backspace as `FilledTonalButton`s of at least `Sizes.minTouchTarget` with `titleLarge` tabular digits, a filled submit `Button` of `Sizes.primaryActionHeight`, masked dots for 4 to 6 digits (`primary` filled, `outlineVariant` empty), keypad anchored in the bottom third, `Spacing` tokens only, content descriptions for TalkBack
- [x] 5.2 Build `UnlockScreen` for `Locked` per design D10: capsule mark and `headlineLarge` heading, the keypad, wrong-PIN error in the `error` role with icon and polite live region, cooldown countdown that disables the keypad, "Use biometrics" as a `FilledTonalButton` when biometric unlock is enabled, automatic prompt on first appearance; `@PreviewLightDark` and `fontScale = 2f` previews
- [x] 5.3 Build the `Recovering` variant of `UnlockScreen`: `bodyLarge` explanation and a filled "Reset app lock" primary action of `Sizes.primaryActionHeight` that runs the device-credential prompt and, on success, calls `ResetLockAfterRecovery`
- [x] 5.4 Reconcile biometric availability when the unlock screen appears, hiding biometric controls and turning the preference off when biometrics are no longer enrolled or hardware is absent
- [x] 5.5 Add all unlock screen strings to `strings.xml`
- [x] 5.6 Compose semantics tests: correct PIN unlocks, wrong PIN clears entry and shows error, fifth failure disables keypad and shows countdown, recovery variant shows the reset action and hides the keypad — `UnlockScreenTest`, 4 tests; all 4 pass on the emulator (see 7.2)

## 6. PIN setup and Security settings section

- [x] 6.1 Build `PinSetupScreen` as a two-step flow (enter, confirm) using `PinKeypad`, as a secondary screen with a `TopAppBar` (`titleLarge` title, back arrow), rejecting fewer than 4 digits with a `bodySmall` minimum-length hint, restarting at step one with a mismatch message in the `error` role when the confirmation differs, and storing nothing until both match
- [x] 6.2 Ensure navigating back or backgrounding during setup leaves the lock disabled and clears any in-memory PIN — no persistence happens until both entries match, so this is a property of the design rather than extra code: leaving the screen (back or process death) simply discards its local state
- [x] 6.3 Build `SecuritySection` for the Settings screen: if the sections list from `app-settings-language` exists, add the section beneath Language; otherwise replace the placeholder with a `LazyColumn` of sections (title in `displayLarge`, section headers in `headlineSmall`) and add Security as its first section. The section holds "Protect with PIN" and "Unlock with biometrics" as `ListItem` rows with a `Switch`, the latter disabled with a `bodySmall` explanation when the PIN lock is off or when no biometric is enrolled or no hardware is present, plus `bodySmall` `onSurfaceVariant` helper text noting that screenshots are blocked while the lock is on — `app-settings-language` had not landed, so the placeholder was replaced
- [x] 6.4 Turning "Protect with PIN" on navigates to `PinSetupScreen`; on success the switch shows on and the lock state becomes `Unlocked`
- [x] 6.5 Turning "Protect with PIN" off opens an `AlertDialog` (`headlineMedium` title, `PinKeypad` in the content, Cancel as `TextButton`); correct PIN calls `DisablePinLock` and clears everything, wrong PIN shows the error and increments the shared failure count, active cooldown refuses entry and shows remaining time; biometrics are never offered here; no `error` colour on the dialog itself because disabling is reversible
- [x] 6.6 Turning "Unlock with biometrics" on runs the biometric prompt and enables the preference only on success; cancel or failure leaves it off
- [x] 6.7 Reconcile biometric availability when the Security section appears
- [x] 6.8 Add all setup and Security section strings to `strings.xml`
- [x] 6.9 Compose semantics tests: setup happy path, mismatch restart, too-short rejection, disable with correct and wrong PIN, biometric switch disabled when PIN lock is off — `PinSetupScreenTest` (3 tests), `SecuritySectionTest` (3 tests); all 6 pass on the emulator (see 7.2)

## 7. Verification and documentation

- [x] 7.1 Run `./gradlew test` and `./gradlew lint` from `src/`; fix failures and report results verbatim — `testDebugUnitTest`: 41 tests, 0 failures. `lintDebug`: 0 findings (fixed two `PluralsCandidate` warnings along the way by pluralizing the cooldown message and rewording the PIN-dots content description)
- [x] 7.2 Run `./gradlew connectedAndroidTest` on a device or emulator for the Keystore and DataStore tests — run on the `pixel_7_-_api_36_0` emulator (API 36): 51 instrumented tests, 0 failures, including `KeystorePinVerifierTest`, `DataStoreAppLockRepositoryTest`, `UnlockScreenTest`, `PinSetupScreenTest`, `SecuritySectionTest` and `PillsnerAppNavigationTest`
- [x] 7.2a Run the `pillsner-ui-review` skill over `applock/ui` and the Settings screen; resolve every finding or list the remaining ones with a reason — 4 findings, all fixed: two raw `0.dp` values in `PinKeypad` (now `Spacing.none`), and three composables missing previews (`PinKeypad`, `SecuritySection`, `SettingsScreen` — the last two lacked a `fontScale = 2f` preview specifically). Re-swept clean afterwards
- [ ] 7.3 Manual test on a physical device with a biometric enrolled: cold start lock, relock after switching apps, relock after screen off, biometric bypass, "Use PIN" fallback, recents thumbnail hidden while enabled and visible when disabled, 200 percent font scale, TalkBack announcement of the wrong-PIN message — still outstanding: needs a physical device with an enrolled biometric and a human observer. The automated emulator run (7.2) covers everything below the biometric prompt; this task stays open for the first device test
- [ ] 7.4 Manual test of the recovery path: enable the lock, delete the Keystore alias via a debug-only hook or `adb` on a debug build, restart, confirm the reset action works and the lock ends up disabled — still outstanding: the app declares no debug hook that deletes the Keystore alias, and an app-private alias cannot be removed over `adb`, so this needs a human on a device. `ResolveInitialLockStateTest` and `KeystorePinVerifierTest` cover the branch that the manual test would exercise
- [x] 7.5 Update `README.md`: add the app lock to the Privacy by default feature list and note that screenshots are blocked while the lock is on; the DI choice is already recorded by `app-welcome-screen` — also added a Fragment row to the Toolchain table (new dependency this change introduces)
- [x] 7.6 Confirm archive order: `app-welcome-screen` before this change — confirmed; `app-welcome-screen`'s own tasks are still in progress in a concurrent session, so this change should not be archived first
